// TypeScript 编译器的配置文件。
// tsconfig.app.json 是为 Angular 应用准备的，
// 而 tsconfig.spec.json 是为单元测试准备的。
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./out-tsc/spec",
    "types": [
      "jasmine",
      "node"
    ]
  },
  "files": [
    "src/test.ts",
    "src/polyfills.ts"
  ],
  "include": [
    "src/**/*.spec.ts",
    "src/**/*.d.ts"
  ]
}
